// public/js/app.js
// Wait for the DOM to be fully loaded before executing the script.
document.addEventListener('DOMContentLoaded', () => {
    console.log('TaskManager JavaScript loaded!');
    // You can add further interactive features here, such as AJAX calls to add tasks.
});
